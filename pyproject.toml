[project]
name = "rep-learning-amazon-reviews"
version = "0.1.0"
description = "Starter repository for large-scale representation learning on Amazon Reviews 2023."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
